# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GoogleCaptions_iOS' do
  platform :ios, '14.5'
  
  use_frameworks!

  pod 'googleapis', :path => '.'

end

target 'GoogleCaptions' do
  platform :macos, '11.3'
  
  use_frameworks!

  pod 'googleapis', :path => '.'

end
